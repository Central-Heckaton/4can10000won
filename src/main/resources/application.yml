spring:
  profiles:
    include:
      - aws
      - credentials

servlet:
  multipart:
    enabled: true
    max-file-size: 20MB
    max-request-size: 20MB

logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace