import React from 'react';
import BeerList from '../components/beerList/beerList';

const Search = (props) => {
    return (
        <div>
            <BeerList />
        </div>
    );
};
export default Search;